# 🧠 Deep Work Tracker

**Минималистичный трекер для глубокой работы с GitHub-style визуализацией.**

Это простой инструмент для тех, кто хочет осознанно отслеживать часы сфокусированной работы (Deep Work). Никаких лишних функций — только таймер, история и наглядная тепловая карта (heatmap), мотивирующая не прерывать цепочку продуктивных дней.

![Скриншот трекера](https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png)
*(Здесь будет скриншот вашего приложения)*

## ✨ Возможности

*   **Таймер фокуса:** Большой таймер, показывающий текущую сессию.
*   **Heatmap активности:** Визуализация прогресса в стиле GitHub (чем больше работаете, тем ярче день).
*   **История сессий:** Лента событий с возможностью добавлять заметки к каждой сессии.
*   **Удаление записей:** Если забыли выключить таймер — лишнюю запись можно удалить.
*   **Облачное хранение:** Все данные надежно хранятся в **Supabase** (PostgreSQL), поэтому история не пропадет при перезагрузке сервера.

## 🚀 Как развернуть (бесплатно)

Этот проект готов к запуску на **Render.com** (или любом другом хостинге с поддержкой Node.js).

### 1. Подготовка базы данных (Supabase)
1. Создайте бесплатный проект на [Supabase.com](https://supabase.com).
2. Зайдите в **SQL Editor** и выполните команду:
   ```sql
   create table sessions (
     id bigint generated by default as identity primary key,
     start_time timestamp with time zone not null,
     end_time timestamp with time zone,
     duration integer,
     note text,
     created_at timestamp with time zone default timezone('utc'::text, now()) not null
   );
   ```
3. Скопируйте **Project URL** и **API Key** (anon/public) из настроек проекта.

### 2. Запуск сайта (Render)
1. Сделайте Fork этого репозитория или загрузите код к себе.
2. Создайте **New Web Service** на [Render.com](https://render.com).
3. Подключите свой репозиторий.
4. В разделе **Environment Variables** добавьте два секрета:
   *   `SUPABASE_URL`: ваш_url_из_пункта_1
   *   `SUPABASE_KEY`: ваш_ключ_из_пункта_1
5. Нажмите **Deploy**.

Готово! Ваш личный трекер доступен по ссылке.

## 🛠 Локальный запуск

1. Клонируйте репозиторий.
2. Установите зависимости: `npm install`.
3. Создайте файл `.env` и пропишите туда ключи Supabase (или отредактируйте `db-supabase.js` для тестов).
4. Запустите: `npm start`.
5. Откройте `http://localhost:3000`.

---
*Created with AI assistance (Cursor + Gemini) for personal productivity.*
